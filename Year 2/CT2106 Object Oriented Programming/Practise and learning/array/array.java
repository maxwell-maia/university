
/**
 * Write a description of class array here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class array
{
    // instance variables - replace the example below with your own
    // array stores primitive variables.
    
    // compared to ArrayList. ArrayList is an object.
    // ArrayList 

    
    //array is a container object.
    
    // fixed number of values;
    
    
    
    // declare;
    int[] numbers;
    
    double[] doubles;
    
    String[] strings;
    
    
    //NUMBER IS [] IS THE NUMBER THAT IT CAN STORE
    public array()
    {
        doubles = new double[4];
        
        
        boolean[] bools = new boolean[4];
        //will fill with default values
        
        // false false false false
        
        //Item[] item = new Item[4];
        // null null null null
        
        
        
        //ALTERNATIVE DECLARE
        
        long[] longArr = {3, 12, 1, 2, 666, 6, 6, 5};
        //longArr.length returns 8
        
        //for array of objects
        //Item[] itemArr = {new Item(), new Item(), new Item()};
    }
    
    // you don't delete . thats for the java garbage collector to do.
    
    // set the index to null and it will dissappear
    
    //Item i1 i2 null
    
    
    
    // Calling
    // indexes start at 0. go up to size -1
    
    
    // LOOPING
    //int i = 0; i < longArr.length; i++
    
    //Student s = new Student();
    //System.out.println(s);
    // to String, implicitly prints the object address
    // make a toString() in the class to print what you want/
    // then say System.out.println(s); or System.out.println(s.toString());
    
    // Enchanced for loop
    // beginning to end of a loop
    // int[] nums = {8, 8, 8};
    
    // for(int value : nums)
    // {
    //    print(value);
    // }
}
    

